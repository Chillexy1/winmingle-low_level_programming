# singly linked lists

## Description
    A singly linked list is a linear data structure where each element (node) contains data and a pointer to the next node.

The goal is to understand what a singly lists are, how to differentiate a singly linked list and an array, how to build a single linked list
