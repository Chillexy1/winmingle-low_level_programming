#include "main.h"

/*
 * main function to call our declared duncts.
 * 
 * must always return 0;
 * */

int main(void){
    int n = 5;
    int a[] = {98, 402, -198, 298, -1024};
    print_array(a, n);

    return(0);
}
